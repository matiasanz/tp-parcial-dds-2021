<!DOCTYPE html>
<html>
<head>
    <title>Local</title>
</head>
<body>
    <H2>{{nombre}}</H2>
    <section id="info-local">
        <h3>Datos del Local:</h3>
        <ul>
            <li><Strong>Direccion: </Strong>{{direccion}}</li>
            <br>
            <li><strong>Categorias:</strong>
                <br>
                <ul>
                    {{#categorias}}
                        <li><a href="/locales?categoria={{this}}">{{this}}</a></li><br>
                    {{else}}
                        <p>Su local no se encuentra asociado a ninguna categoría</p>
                    {{/categorias}}
                </ul>
            </li>
        </ul>
    </section>

    <section id="Menu">
        <h3>Menu:</h3>
        <ul>
            {{#platos}}
                <li><a href="/locales/{{../idLocal}}/platos/{{idPlato}}">{{nombre}}</a> ........... ${{precio}}</li>
            {{else}}
                <p>Aún no se registraron platos</p>
            {{/platos}}
        </ul>
    </section>